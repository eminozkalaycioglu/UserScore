# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UserScoreFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase'

pod 'Firebase/Auth'
pod ‘Firebase/Core’
pod ‘Firebase/Database’


  # Pods for UserScoreFirebase

  target 'UserScoreFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UserScoreFirebaseUITests' do
    # Pods for testing
  end

end
